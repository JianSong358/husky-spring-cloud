server:
  tomcat:
    uri-encoding: UTF-8
  port: 6003
spring:
  profiles:
    active: dev
  application:
    name: mopsz-soft-redis
  http:
    encoding:
      charset: UTF-8
      force: true
      enabled: true